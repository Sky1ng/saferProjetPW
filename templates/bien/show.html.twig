{% extends 'base.html.twig' %}

{% block title %}Bien{% endblock %}

{% block body %}
    <h1>Bien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bien.id }}</td>
            </tr>
            <tr>
                <th>Vente_location</th>
                <td>{{ bien.venteLocation }}</td>
            </tr>
            <tr>
                <th>Surface</th>
                <td>{{ bien.surface }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ bien.type }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ bien.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bien_index') }}">back to list</a>

    <a href="{{ path('app_bien_edit', {'id': bien.id}) }}">edit</a>

    {{ include('bien/_delete_form.html.twig') }}
{% endblock %}
