{% extends 'base.html.twig' %}

{% block title %}Hello FavorisController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>FAVORIS ! ✅</h1>

    Vos favoris sont les suivants
</div>

    {% for bien in biens %}
        <td>{{ bien.id }}</td>
        <td>{{ bien.reference }}</td>
        <td>{{ bien.titre }}</td>
        <td>{{ bien.surface }}</td>
        <td>{{ bien.type }}</td>
        <td>{{ bien.prix }}</td>
        <td><a href="{{ path('app_admin_removeById', {id: bien.id}) }}"> Supprimer le bien des favoris</a></td>

        <br>
        <br>
    {% endfor %}
    <br>
    <br>
    <a href="{{ path('app_admin_remove') }}"><button>Retirer tous les favoris</button> </a>


    <br>
    <br>
    <form action="{{ path('app_admin_mail') }}" method="post">
        <input type="email" name="email" id="email" placeholder="votre mail">

        <button type="submit">Envoyer les favoris par mail</button>
    </form>

{% endblock %}
