<form method="post" action="{{ path('app_favoris_sent_delete', {'id': favoris_sent.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favoris_sent.id) }}">
    <button class="btn">Delete</button>
</form>
